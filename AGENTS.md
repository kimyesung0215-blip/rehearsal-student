# rehearsal-student — AGENTS.md

## 프로젝트 목적
수업 리허설용 가상 학생. 화자(예성)의 음성에 실시간 반응하는 아바타 + 세션 녹음/러프 전사.
**학생을 가르치는 도구가 아니라, 화자의 이해도 구멍을 드러내는 검출 파이프라인의 입력 장치.**
평가 자체는 앱 밖(전사본 → Claude 페르소나 평가)에서 수행한다.

## 아키텍처 — 2층 반응 구조
- **1층 (useVAD)**: Web Audio RMS 기반 음성 활동 감지. AI/네트워크 없음. 오프라인 보장.
  speaking → 끄덕 / silence_short → 갸웃 / silence_long → "...?"
- **2층 (useSpeechCues)**: Web Speech API interim 자막 → 정규식 패턴 매칭 → 반응 이벤트.
  STT는 반응 트리거 전용. 끊기면 자동 재시작, 실패 시 조용히 1층으로 강등.
  부산물로 러프 전사를 모아 세션 종료 시 .txt 제공. 정밀 전사는 녹음 파일 → 클로바노트.
- 상태 우선순위: 디버그 > 2층 큐(2.6초 후 복귀) > 1층 VAD.

## 규칙
- 모든 튜닝 값(임계값, 대기시간, 패턴)은 `src/config/tuning.js`에만 둔다. 매직넘버 금지.
- 마이크 스트림은 getUserMedia 1회 → VAD와 MediaRecorder가 공유. 이중 요청 금지.
- 외부 런타임 의존성 추가 금지 (react, react-dom만). 번들 최소 유지.
- 세션 데이터는 저장하지 않는다. 다운로드 링크 제공 후 휘발. (히스토리 기능은 명시적 결정 전까지 스코프 밖)
- 결정 변경 시 docs/decisions.md에 기록.

## 스코프 밖 (v0.1에서 의도적으로 제외)
평가 자동화(LLM API 호출), 세션 히스토리, 설정 UI, 페르소나 UI, 계정.
